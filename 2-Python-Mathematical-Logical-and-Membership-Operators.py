# Задание 1: Переприсваивание значений переменных
print("Задание 1: Переприсваивание значений переменных a и b")
a = 5  # Инициализируем переменную a значением 5
b = 10  # Инициализируем переменную b значением 10
print(f"Начальные значения: a = {a}, b = {b}")
a = b  # Присваиваем a значение b (a становится 10)
print(f"После присваивания a = b: a = {a}")
b = 20  # Переприсваиваем b значение 20
print(f"После присваивания b = 20: b = {b}")
print(f"Итоговые значения переменных: a = {a}, b = {b}")
print("-" * 50)

# Задание 2: Арифметические операции с переменными x, y, z
print("Задание 2: Выполнение арифметических операций с переменными x, y, z")
x = 7  # Инициализируем x значением 7
y = 3  # Инициализируем y значением 3
z = 0  # Инициализируем z значением 0
print(f"Исходные значения: x = {x}, y = {y}, z = {z}")

# 1. Сумма x и y
sum_result = x + y
print(f"1. Сумма x + y: {x} + {y} = {sum_result}")

# 2. Разность x и y
diff_result = x - y
print(f"2. Разность x - y: {x} - {y} = {diff_result}")

# 3. Умножение x на y
mul_result = x * y
print(f"3. Умножение x * y: {x} * {y} = {mul_result}")

# 4. Деление x на y с остатком
mod_result = x % y
print(f"4. Остаток от деления x % y: {x} % {y} = {mod_result}")

# 5. Целочисленное деление x на y
floor_div_result = x // y
print(f"5. Целочисленное деление x // y: {x} // {y} = {floor_div_result}")

# 6. Возведение x в степень y
power_result = x ** y
print(f"6. Возведение x в степень y: {x} ** {y} = {power_result}")

# 7. Вычисление z = x**2 - (x * y)
z = x**2 - (x * y)
print(f"7. Вычисление z = x**2 - (x * y): {x}**2 - ({x} * {y}) = {z}")
print("-" * 50)

# Задание 3: Составные операторы присваивания
print("Задание 3: Использование составных операторов присваивания с m и n")
m = 8  # Инициализируем m значением 8
n = 4  # Инициализируем n значением 4
print(f"Исходные значения: m = {m}, n = {n}")

# 1. m = m + n
m += n
print(f"1. После m += n: m = {m} (было {m-n}+{n})")

# 2. n = m * 2
n = m * 2
print(f"2. После n = m * 2: n = {m} * 2 = {n}")

# 3. m = n % m
m = n % m
print(f"3. После m = n % m: m = {n} % {m+n} = {m}")

# 4. n = n - m
n = n - m
print(f"4. После n = n - m: n = {n+m} - {m} = {n}")

# 5. Вывод итоговых значений
print(f"Итоговые значения: m = {m}, n = {n}")
print("-" * 50)

# Задание 4: Операторы сравнения
print("Задание 4: Использование операторов сравнения с a, b, c")
a = 12  # Инициализируем a значением 12
b = 8   # Инициализируем b значением 8
c = 12  # Инициализируем c значением 12
print(f"Исходные значения: a = {a}, b = {b}, c = {c}")

# 1. Проверка a > b
print(f"1. Проверка a > b: {a} > {b} = {a > b}")

# 2. Проверка b < a
print(f"2. Проверка b < a: {b} < {a} = {b < a}")

# 3. Проверка a == c
print(f"3. Проверка a == c: {a} == {c} = {a == c}")

# 4. Проверка b != c
print(f"4. Проверка b != c: {b} != {c} = {b != c}")

# 5. Проверка a >= c
print(f"5. Проверка a >= c: {a} >= {c} = {a >= c}")

# 6. Проверка b <= a
print(f"6. Проверка b <= a: {b} <= {a} = {b <= a}")
print("-" * 50)

# Задание 5: Операторы членства и длина списка
print("Задание 5: Проверка операторов членства и длины списка backpack")
backpack = ['ручка', 'тетрадь', 'учебник', 'карандаш', 'фломастеры', 'линейка', 'ластик']
print(f"Содержимое рюкзака: {backpack}")

# 1. Проверка наличия карандаша
is_pencil = 'карандаш' in backpack
print(f"1. Есть ли карандаш в рюкзаке? {'карандаш' in backpack}")

# 2. Проверка отсутствия яблока
no_apple = 'яблоко' not in backpack
print(f"2. Нет ли яблока в рюкзаке? {'яблоко' not in backpack}")

# 3. Длина рюкзака
length = len(backpack)
print(f"3. Количество элементов в рюкзаке: {len(backpack)}")
print("-" * 50)
